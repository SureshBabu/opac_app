<tr>
<td><%= ibtrversion.version %></td>
<td><%= ibtrversion.title_id  %></td>
<td><%= ibtrversion.state %></td>
<td>
<%=
	case 
	when ibtrversion.state.eql?('Assigned') then 'to'
	when ibtrversion.state.eql?('Declined') then 'by'
	when ibtrversion.state.eql?('Fulfilled') then 'by'
	when ibtrversion.state.eql?('Dispatched') then 'by'
	when ibtrversion.state.eql?('Received') then 'by'
	when ibtrversion.state.eql?('Cancelled')
	end
%>
</td>
<td><%= ibtrversion.respondent_id unless ibtrversion.state.eql?('Cancelled') %></td>
<td><%= distance_of_time_in_words(ibtrversion.updated_at, Time.now) %> ago</td>
</tr>