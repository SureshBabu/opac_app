<h2>Forgot your password?</h2>

<p> For us to mail you instructions to reset your password, type the full email address that you used to signup. </p>

<%= form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post }) do |f| %>
  <%= devise_error_messages! %>

  <p><%= f.label :email %><br />
  <%= f.text_field :email %></p>

  <p><%= f.submit "Submit" %></p>
<% end %>

<%- if devise_mapping.registerable? && controller_name != 'registrations' %>
  <p> If you don't have an account with us, <%= link_to "Sign up", new_registration_path(resource_name) %> now.</p>
<% end -%>
